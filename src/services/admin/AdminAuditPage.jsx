import React,{useEffect,useState} from "react";
import {getCommandAudit} from "./adminCommandCenter";
export default function AdminAuditPage(){const[data,setData]=useState({rows:[],total:0}),[page,setPage]=useState(0);useEffect(()=>{let cancelled=false;const load=async()=>{try{const result=await getCommandAudit(100,page*100);if(!cancelled)setData(result);}catch(e){if(!cancelled)alert(e.message);}};load();return()=>{cancelled=true;};},[page]);return <div className="admin-page"><div className="admin-page-title"><div><div className="admin-breadcrumb">Administration <span>/</span> Audit Logs</div><h1>Audit Log</h1><p>Immutable operational history: actor, action, resource, old value and new value.</p></div></div><section className="admin-panel admin-data-table"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Time</th><th>Admin</th><th>Action</th><th>Resource</th><th>Details</th></tr></thead><tbody>{data.rows.map(r=><tr key={r.id}><td>{new Date(r.created_at).toLocaleString()}</td><td>{r.actor_name}</td><td>{r.action}</td><td>{r.resource_type} / {r.resource_id||"—"}</td><td><code>{JSON.stringify(r.metadata)}</code></td></tr>)}</tbody></table></div><div className="admin-data-foot"><span>{data.total} events</span><div className="admin-pager"><button disabled={!page} onClick={()=>setPage(page-1)}>Previous</button><button disabled={(page+1)*100>=data.total} onClick={()=>setPage(page+1)}>Next</button></div></div></section></div>}
